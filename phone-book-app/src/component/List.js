import React, {Component} from 'react';
import PropTypes from 'prop-types';

class List extends Component {
    state = {};
    static defaultProps = {
        
    };

    static propTypes = {};

    render() {
        return (
            <div></div>
        );
    }
}

export default List;
